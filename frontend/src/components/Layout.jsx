/**
 * Layout (placeholder)
 * A gente vai “ligar” isso no App.jsx depois SEM alterar o layout aprovado.
 * Por enquanto, é só um wrapper para facilitar evolução modular.
 */
export default function Layout({ children }) {
  return <>{children}</>;
}
