# Porta padrão do servidor
PORT=4000

# URL do banco de dados (a definir: Postgres/Neon, etc.)
DATABASE_URL=postgres://user:password@localhost:5432/controles_amr

# Chave de sessão / JWT (para login)
JWT_SECRET=trocar_esta_chave_em_producao